public class ForeverUI : ForeverUIBase
{
    public override string Key => nameof(ForeverUI);
    
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
